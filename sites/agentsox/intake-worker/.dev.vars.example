# Optional local overrides for `npm run dev`.
# Keep real secrets in `.dev.vars`, never in git.
# The default local environment already uses MODEL_PROVIDER=local-preview.

SITE_ID=agentsox-main
MODEL_PROVIDER=local-preview
WORKERS_AI_MODEL=@cf/qwen/qwen3-30b-a3b-fp8
ALLOWED_ORIGINS=http://localhost:5174,http://127.0.0.1:5174
CONTACT_ENDPOINT=http://127.0.0.1:8788/api/contact
