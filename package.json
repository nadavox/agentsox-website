{
  "name": "agentsox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "workers/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @agentsox/web run dev",
    "build": "npm --workspace @agentsox/web run build",
    "preview": "npm --workspace @agentsox/web run preview",
    "lint": "npm --workspace @agentsox/web run lint",
    "typecheck": "npm --workspace @agentsox/intake-worker run typecheck && npm --workspace @agentsox/mail-worker run typecheck",
    "verify": "npm run lint && npm run build && npm run typecheck"
  }
}
